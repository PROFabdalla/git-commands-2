task githup from abdalla mohamed abdalla amer
track python

git init
git config --global user.name "PROFabdalla"
git config --global user.email "abdallaamer101@gmail.com"
git config --global init.defaultBranch main
git config pull.rebase false
git status


ssh-keygen -t rsa -b 4096 -C "abdallaamer101@gmail.com"

git remote add origin git@github.com:PROFabdalla/abdalla2.git

touch file abdalla2


git branch dev 
 git checkout dev
git checkout dev
touch abdalla2  #file
git add .
git commit -m "touching file from branch"
git push origin dev




git checkout main

git merge dev




